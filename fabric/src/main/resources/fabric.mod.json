{
  "schemaVersion": 1,
  "id": "example",
  "version": "${modVersion}",

  "name": "${modName}",
  "description": "${modDescription}",
  "authors": ["${modAuthor}"],
  "contact": {
    "homepage": "${modIssueTracker}"
  },

  "license": "${modLicense}",
  "icon": "icon.png",

  "environment": "*",
  "entrypoints": {},
  "mixins": [
    "example-fabric.mixins.json",
    "example-common.mixins.json"
  ],
  "depends": {
    "fabricloader": "${fabricLoaderRange}",
    "minecraft": "${fabricMinecraftVersionRange}",
    "architectury": "${fabricArchitecturyVersionRange}"
  }
}
