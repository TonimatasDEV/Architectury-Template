{
  "schemaVersion": 1,
  "id": "example",
  "version": "${mod_version}",

  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": ["${mod_author}"],
  "contact": {
    "homepage": "${mod_issueTracker}"
  },

  "license": "${mod_license}",
  "icon": "icon.png",

  "environment": "*",
  "entrypoints": {},
  "mixins": [
    "example.mixins.json",
    "example-common.mixins.json"
  ],
  "depends": {
    "fabricloader": "${fabric_loader_range}",
    "minecraft": "${fabric_minecraft_version_range}",
    "architectury": "${fabric_architectury_version_range}"
  }
}
